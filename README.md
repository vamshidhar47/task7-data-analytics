# Task 7 – Basic Sales Summary with SQLite + Python

## Objective
Use SQL inside Python to extract a basic sales summary and visualize results with a bar chart.

## Tools Used
- SQLite (built-in)
- Python (sqlite3, pandas, matplotlib)

## Steps
1. Created a SQLite database file named `sales_data.db`
2. Inserted sample product sales data
3. Connected Python to the database using sqlite3
4. Ran a SQL query with GROUP BY to calculate total quantity and revenue
5. Loaded results into pandas and printed the table
6. Created a simple bar chart using matplotlib

## Output
- Printed sales summary table
- Bar chart showing revenue by product
